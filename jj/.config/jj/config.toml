"$schema" = "https://jj-vcs.github.io/jj/latest/config-schema.json"

[user]
name = "Justin Li"
email = "justinnhli@gmail.com"

[git]
colocate = false

[ui]
diff-editor = ":builtin"

[revset-aliases]
"closest_bookmark(to)" = "heads(::to & bookmarks())"
"hidden_bookmarks()" = "bookmarks(regex:'^hidden-')"
"visible_bookmarks()" = "bookmarks() ~ hidden_bookmarks()"
"non_hidden_heads()" = "heads(::) ~ hidden_bookmarks() ~ untracked_remote_bookmarks()"
"closest_immutable(to)" = "heads(::to & immutable_heads())"
"log_revset()" = "(@--::) | closest_immutable(@) | non_hidden_heads() | visible_bookmarks() | conflicts() | @::"

[revsets]
log = "fork_point(log_revset()) | log_revset()"

[aliases]
clone = ["git", "clone"]
gc = ["util", "gc"]
fetch = ["git", "fetch"]
"new+" = ["new", "--insert-after", "@"]
"new-" = ["new", "--insert-before", "@"]
loga = ["log", "-r", "::"]
logn = ["log", "-r", "present(@) | ancestors(immutable_heads().., 2) | present(trunk())"]
push = ["git", "push"]
tug = ["bookmark", "move", "--from", "closest_bookmark(@-)", "--to", "@-"]

[templates]
draft_commit_description = '''
concat(
  builtin_draft_commit_description,
  "\nJJ: ignore-rest\n",
  diff.git(),
)
'''
