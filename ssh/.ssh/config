Host autograder
	Hostname autograder.cc.oxy.edu

Host bletchley
	Hostname bletchley.oxy.edu

Host dreamhost
	Hostname justinnhli.com


# if on the local network, use a local IP 
# we're on the local network if
# * we're on the 192.168.1.0/24 subnet (find subnet information with `ip addr show`)
# * the machine is can be pinged (condition disabled)
#Match host home localnetwork 192.168.1.0/24 exec "ping -i 0.25 -w 0.1 -c 2 192.168.1.64"
Match host home localnetwork 192.168.1.0/24
	Hostname 192.168.1.64
# otherwise, default to a global IP
Host home
	Hostname 107.207.48.65


Host spark
	Hostname 192.168.1.64

Host envy
	Hostname 192.168.1.127


Host *
	Compression yes
	User justinnhli
